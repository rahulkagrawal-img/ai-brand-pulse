{
  "name": "ai-brand-pulse",
  "version": "0.1.0",
  "private": true,
  "description": "Deterministic scoring engine for the AI Brand Pulse commerce-visibility audit",
  "type": "module",
  "engines": {
    "node": ">=22.6.0"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --experimental-strip-types --test 'src/**/*.test.ts'",
    "validate:fixtures": "node --experimental-strip-types scripts/validate-fixtures.ts",
    "check": "npm run typecheck && npm run validate:fixtures && npm test"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "zod": "^3.24.1"
  }
}
